function setup() {
  let myCanvas = createCanvas(400, 400);
  frameRate(30);
}

window.addEventListener("resize", function () {
  resizeCanvas(400, 400);
  clear();
});

new p5();
var width = 400;
var height = 400;