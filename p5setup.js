function setup() {
  let myCanvas = createCanvas(750, 400);
  frameRate(30);
}

window.addEventListener("resize", function () {
  resizeCanvas(750, 400);
  clear();
});

new p5();
var width = 750;
var height = 400;
